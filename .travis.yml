language: node_js
node_js:
  - node
before_deploy: echo deployment is starting
deploy:
  provider: cloudfoundry
  username: admin
  password: $CF_PASSWORD
  api: $CF_API --skip-ssl-validation
  organization: sampleOrg
  space: dev
  app_name: exchange-value
  edge: true
after_deploy:
  - cf apps
